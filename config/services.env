# Legacy service connection (planted secret for the scan).
# A URL with embedded basic-auth credentials. TruffleHog detects this and
# verifies it is live, which turns the secret check red.
LEGACY_BILLING_API = "https://admin:admin@the-internet.herokuapp.com/basic_auth"
